class Telebook:
    def __init__(self):
        self.telebook = {}

    def add(self, name=None, number=None):
        print "add inne"


    while True:
        user_input = raw_input("telebok> ")
        user_input = user_input.lower()
        user_input = user_input.split()

        print user_input

        try:
            user_input = getattr(user, user_input[0])(*user_input[1:])
        except AttributeError:
            print "......"

        print user_input

##        if user_input[0] == "add":
##            print "im in"
##            print ""
##        else:
##            print "Error: The function you are trying to use doesn't exist,"
##            print "Functions avalble for use are: add, lookup, alias, change, save, load and quit."
##            print ""
##            continue
